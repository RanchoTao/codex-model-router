# Optional cost-saving parent/orchestrator configuration.
# Merge manually into ~/.codex/config.toml; do not overwrite unrelated settings.

model = "gpt-5.6-luna"
model_reasoning_effort = "low"

[agents]
enabled = true
max_concurrent_threads_per_session = 4
